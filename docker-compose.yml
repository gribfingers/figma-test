services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - backend_data:/app/data
    environment:
      - PORT=4000

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend_data:
